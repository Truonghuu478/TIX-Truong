import React from 'react'
import {useSelector} from "react-redux"
export default function CountTicketM() {
    const {listChairs} = useSelector(state.UserReducer)
    return (
        <div>
            
        </div>
    )
}
